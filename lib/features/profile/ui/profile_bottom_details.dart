import 'package:flutter/material.dart';

class ProfileBottomDetails extends StatelessWidget {
  const ProfileBottomDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
